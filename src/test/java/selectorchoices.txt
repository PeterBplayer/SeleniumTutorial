1. By.id UWAGA: trzeba sprawdzić, czy id nie jest za każdym razem automatycznie generowane
i nie zmienia się za każdym ponowny załadowaniu strony.
2. By.name UWAGA: trzeba potwierdzić unikalność wartości atrybutu name w konsoli DevTools
3. By.linkText & By.partialLinkText UWAGA: tylko dla lokalizowania linków
4. selektory CSS (w konsoli DevTools $$(""))
5. selektory Xpath